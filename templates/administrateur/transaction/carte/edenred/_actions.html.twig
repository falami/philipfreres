{# templates/administrateur/transaction/carte/edenred/_actions.html.twig #}

<div class="btn-group btn-group-sm user-actions" role="group">

  {# ✅ Bouton principal = Modifier #}
  <a class="btn btn-light user-main"
     data-bs-toggle="tooltip" title="Modifier"
     href="{{ path('app_administrateur_tce_edit', { entite: entite.id, id: t.id }) }}">
    <i class="bi bi-pencil"></i>
  </a>

  {# ✅ Split dropdown #}
  <button type="button"
          class="btn btn-light dropdown-toggle dropdown-toggle-split user-split"
          data-bs-toggle="dropdown"
          data-bs-auto-close="outside"
          aria-expanded="false">
    <span class="visually-hidden">Actions</span>
  </button>

  <ul class="dropdown-menu dropdown-menu-end user-menu">

    {# ✅ Afficher #}
    <li>
      <a class="dropdown-item d-flex align-items-center"
         href="{{ path('app_administrateur_tce_show', { entite: entite.id, id: t.id }) }}">
        <i class="bi bi-eye me-2"></i> Afficher
      </a>
    </li>

    <li><hr class="dropdown-divider"></li>

    {# ✅ Supprimer (modal) #}
    <li>
      <button type="button"
              class="dropdown-item d-flex align-items-center text-danger delete-btn"
              data-bs-toggle="modal" data-bs-target="#deleteModal"
              data-url="{{ path('app_administrateur_tce_supprimer', { entite: entite.id, id: t.id }) }}"
              data-token="{{ csrf_token('delete_tce_' ~ t.id) }}">
        <i class="bi bi-trash me-2"></i> Supprimer
      </button>
    </li>

  </ul>
</div>