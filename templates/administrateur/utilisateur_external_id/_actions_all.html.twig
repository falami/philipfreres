<div class="btn-group ext-actions">
  <a class="btn btn-sm btn-light fw-semibold"
     href="{{ path('app_administrateur_utilisateur_external_id_edit', { entite: entite.id, utilisateur: utilisateur.id, id: item.id }) }}">
    <i class="bi bi-eye"></i>
  </a>

  <button class="btn btn-sm btn-light dropdown-toggle dropdown-toggle-split"
          data-bs-toggle="dropdown" aria-expanded="false">
    <span class="visually-hidden">Menu</span>
  </button>

  <ul class="dropdown-menu dropdown-menu-end">
    <li>
      <a class="dropdown-item"
         href="{{ path('app_administrateur_utilisateur_modifier', { entite: entite.id, id: utilisateur.id }) }}">
        <i class="bi bi-person me-2"></i> Voir l'utilisateur
      </a>
    </li>
    <li>
      <a class="dropdown-item"
         href="{{ path('app_administrateur_utilisateur_external_id_index', { entite: entite.id, utilisateur: utilisateur.id }) }}">
        <i class="bi bi-plug me-2"></i> External IDs
      </a>
    </li> 
  </ul>
</div>